import Typed from "typed.js";

// const tupangOptions = {
//   strings: ["HELLO TUPANG ^1000 :)"],
//   typeSpeed: 40,
//   startDelay: 1000,
// };

// const tupang = new Typed(".js-tupang-title", tupangOptions);

const naborOptions = {
  strings: ["meybi ^1000 it was you ^800? ^1500 nabor today?."],
  typeSpeed: 40,
  startDelay: 3000,
  fadeOutClass: "typed-fade-out",
  fadeOutDelay: 500,
  showCursor: true,
  cursorChar: "|",
  autoInsertCss: true,
};

const nabor = new Typed(".js-typed-text", naborOptions);
